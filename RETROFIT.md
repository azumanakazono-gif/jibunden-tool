# 卒FIT・蓄電池後付けモード

編集ロックを解除して「蓄電池のみ（後付け）」を選択すると専用画面が開きます。ロックは継続し、JSON・履歴から読み込んでも自動解除しません。従来の新設向け入力値は保持します。

## 入力と比較

直近12か月の既設PV発電量・売電量・買電量・買電料金、放電時間帯の買電従量単価、売電契約の単価を入力します。基本料金を従量単価に含めません。卒FIT済みの場合はFIT残存年数を0にします。残存年数は整数年の概算です。

実効容量は放電側の利用可能エネルギーを入力します。公称容量とは異なるためメーカー資料で確認します。割合や年数の初期値は計算上の仮定であり、メーカー性能値・保証年数ではありません。

- 放電量 = min(年間売電量 × 充電可能割合 × 往復効率, 実効容量 × 日常利用割合 × 稼働日数 × 経年容量係数, 年間買電量 × 置換可能割合, (年間買電料金 − 年間基本料金等) ÷ 買電従量単価)
- 充電量 = 放電量 ÷ 往復効率
- 追加メリット = 放電量 × 買電従量単価 − 充電量 × 売電単価 − 維持費 − 該当年の交換費
- 投資差引累計 = 追加メリットの累計 − (税込総工事費 − 補助金)

FIT残存期間中はFIT単価、その後は卒FIT単価を適用します。既設PVの購入費用と既存の直接自家消費効果は追加効果に加算しません。容量低下は蓄電池にのみ適用し、交換費を計上しても容量回復は見込まない保守的な計算です。初回黒字化の年数は年内均等の概算で、後年の交換費による再赤字化を排除しません。

## 保存・出力

後付け入力はJSON内の `batteryRetrofit` に保存します。お客様履歴、JSON読込、再読込で復元します。専用PPTXには前提、経済効果、設置確認事項、年次収支を出力します。新設向けAIコメントは使いません。

## 対象外

年次集計による概算です。天候・発電と需要の時間差・出力制限は入力割合で簡略化し、30分値によるシミュレーションは行いません。買電・売電単価と既設PV発電量は一定。夜間系統充電、プラン変更、金利、税効果を含みません。停電対策は経済効果と分けて説明してください。

## 検証

`node --test tests/retrofit.test.cjs`

GitHub Actionsの `Battery retrofit checks` は計算テストとブラウザ操作を検証します。画面キャプチャと出力ファイルは `retrofit-review` アーティファクトに保存します。
